services:
  - type: web
    name: fynd-ai-dashboard
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app.py
    envVars:
      - key: GEMINI_API_KEY
        sync: false